# LigandMPNN Skill

设计面上还带着配体、核酸或金属时，反向折叠该怎么做。LigandMPNN 把这些原子一并放进图里，普通 ProteinMPNN 看不见的东西它能看见。它的 `run.py` 还会把设计好的序列贴回输入坐标，把 PDB 和 FASTA 一起写出来——所以哪怕是纯蛋白任务，用它当 runner 也更省事，ProteinMPNN 权重和可溶权重它同样接得住。这份 recipe 只指向外部的 LigandMPNN 仓库和 checkpoint，不捆绑可执行文件，也不捆绑权重。

LigandMPNN 是围着你交给它的那份配体坐标做设计的，而且它把这个位姿当成事实。要是喂进去的是对接或建模摆上去、位置有偏差的配体，设计出来的口袋就是照着一个可能并不真实的配体位置长的——而这一次运行看上去和一次正确的运行毫无分别。每条序列头里的 `ligand_confidence`，是模型给自己刚写出来的序列打的分，不是对结合的测量。链怎么选、固定哪些残基、带不带上下文原子、用哪种 model type，都必须对着真实的输入结构核对；生成的序列和贴回坐标得到的结构，在实验给出结论之前都只是设计候选。

## 文件

| 文件 | 职责 |
| --- | --- |
| [`SKILL.md`](SKILL.md) | 说明仓库怎么装、有哪几种 model type 及其对应权重、PDB 与配体上下文怎么解析、哪些链和残基参与设计或被固定、采样与批量输出怎么配、贴回坐标的 PDB 在哪里。配体相关的坑也在里面，例如输入 PDB 的 HETATM 被上游清理掉之后，模型看不见口袋却不会报错。下游验证同样有交代。 |
